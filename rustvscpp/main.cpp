#include <iostream>
#include <vector>
using namespace std;





int main()
{
    vector<int> res;
    res.push_back(100);
    res.push_back(200);
    res.push_back(300);
    res.push_back(400);
    res.push_back(500);
}